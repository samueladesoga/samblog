version: 2
jobs:
  build:
    docker:
      - image: alteroo/hugo-and-firebase
    working_directory: ~/samblog
    steps:
      - checkout
      - run: hugo
      - run: |
          [ "${CIRCLE_BRANCH}" == "master" ] && firebase deploy --token ${FIREBASE_TOKEN} --non-interactive